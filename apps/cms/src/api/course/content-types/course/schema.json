{
  "kind": "collectionType",
  "collectionName": "courses",
  "info": {
    "singularName": "course",
    "pluralName": "courses",
    "displayName": "Course",
    "description": "Training course (issue #29): ordered lessons, mandatory completion tracking. Maintained in the Strapi admin (no frontend authoring in v1); native draft & publish drives visibility. completionMode: confirm = Abschluss per Klick; quizGate = alle Quizfragen richtig (Retry erlaubt) schalten den Abschluss frei. Geplante v2-Werte (dokumentiert, NICHT bauen ohne eigenes Design): exam, plus validityMonths fuer wiederkehrende Unterweisungen."
  },
  "options": {
    "draftAndPublish": true
  },
  "attributes": {
    "title": {
      "type": "string",
      "required": true
    },
    "slug": {
      "type": "uid",
      "targetField": "title",
      "required": true
    },
    "description": {
      "type": "text"
    },
    "mandatory": {
      "type": "boolean",
      "default": true
    },
    "completionMode": {
      "type": "enumeration",
      "enum": ["confirm", "quizGate"],
      "default": "confirm"
    },
    "coverImage": {
      "type": "media",
      "multiple": false,
      "allowedTypes": ["images"]
    },
    "lessons": {
      "type": "relation",
      "relation": "oneToMany",
      "target": "api::lesson.lesson",
      "mappedBy": "course"
    }
  }
}
